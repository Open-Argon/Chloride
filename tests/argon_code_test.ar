

let code = {}

code.hello_world() = do
  term.log("hello world from argon code")

code.int_add(a,b) = do
  return a+b

code.double_add(a,b) = do
  return a+b

code.rational_add(a,b) = do
  return a+b

code.yooo(name) = do
  return "Yooo "+name+", how are you?"

term.log(code)

code.hello_world()

term.log(code.int_add(100,200))
term.log(code.double_add(0.1,0.2))
term.log(code.rational_add(1/2,1/3))

term.log(code.yooo("Chloride"))